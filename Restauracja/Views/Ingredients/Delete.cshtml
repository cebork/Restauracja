@model Restauracja.Models.Ingredient

@{
    ViewData["Title"] = "Składniki";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Ingredient</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.IsDeleted)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.IsDeleted)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="IngredientID" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>


@model Restauracja.Models.Category

@{
    ViewData["Title"] = "Skłladniki";
}*@

<div class="container-fluid">
    <div class="row d-flex justify-content-center">
        <div class="d-flex justify-content-center">
            <h1>Usuwanie składników</h1>
        </div>
        <div class="d-flex justify-content-center">
            <h3>Czy napewno chcesz usunąć składnik o nazwie <b>@(Model.Name)</b>?</h3>
        </div>
        <div class="d-flex justify-content-center">
            <form asp-action="Delete">
                <input type="hidden" asp-for="IngredientID" />
                <input type="submit" value="Usuń" class="btn btn-danger" />                
            </form>
            <a asp-action="Index"><button class="btn btn-primary"> Wróć</button></a>
        </div>
    </div>
</div>
